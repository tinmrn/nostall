name: Build
on: [ push ]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./build.sh
      - if: ${{ github.ref_type == 'tag' }}
        run: gh release create ${{github.ref_name}} ./.bin/*
        env:
          GH_TOKEN: ${{ github.token }}